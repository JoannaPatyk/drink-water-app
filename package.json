{
    "dependencies": {
        "gh-pages": "^5.0.0",
        "prettier": "^2.8.7",
        "typescript": "^5.0.3"
    },
    "scripts": {
        "compile": "tsc",
        "compile-watch": "tsc --watch",
        "prettier-check": "npx prettier --check .",
        "prettier-fix": "npx prettier --write ."
    }
}
